# The read model: one denormalized row per movie, folded from four topics.
#   - movie  -> the spine (descriptive columns; its delete drops the row)
#   - rating -> a contributor (its columns; its delete clears them, row stays)
#   - person -> a dimension (person_id -> name), enriched into the arrays
#   - credit -> split by role into top_cast (actors) and directors,
#               each element enriched with the person's name
# See README § SQL projections for the full mechanics.
[syncable]
name = "movie-card"
type = "projection"

[projection]
db = "bff"
table = "movie_card"
primaryKey = "movie_id"

[[projection.columns]]
name = "movie_id"
type = "VARCHAR(16)"
[[projection.columns]]
name = "title"
type = "VARCHAR(255)"
[[projection.columns]]
name = "year"
type = "INT"
[[projection.columns]]
name = "genres"
type = "VARCHAR(255)"
[[projection.columns]]
name = "score"
type = "NUMERIC"
[[projection.columns]]
name = "votes"
type = "INT"
[[projection.columns]]
name = "top_cast"
type = "JSONB"
[[projection.columns]]
name = "directors"
type = "JSONB"

[[projection.source]]
topic = "movie"
onDelete = "delete-row"
[[projection.source.rules]]
set = [
  { column = "title", from = "$.title" },
  { column = "year", from = "$.year" },
  { column = "genres", from = "$.genres" },
]

[[projection.source]]
topic = "rating"
onDelete = "clear"
[[projection.source.rules]]
set = [
  { column = "score", from = "$.score" },
  { column = "votes", from = "$.votes" },
]

[[projection.source]]
topic = "person"
[projection.source.lookup]
name = "people"
[[projection.source.lookup.field]]
field = "name"
from = "$.name"

[[projection.source]]
topic = "credit"
keyPath = "$.movie_id"
when = [ { path = "$.role", equals = "actor" } ]
[projection.source.aggregate]
column = "top_cast"
elementKey = "$.billing"
elementKeyType = "number"
[[projection.source.aggregate.element]]
field = "billing"
from = "$.billing"
[[projection.source.aggregate.element]]
field = "person_id"
from = "$.person_id"
[[projection.source.aggregate.element]]
field = "name"
lookup = "people"
on = "person_id"
select = "name"

[[projection.source]]
topic = "credit"
keyPath = "$.movie_id"
when = [ { path = "$.role", equals = "director" } ]
[projection.source.aggregate]
column = "directors"
elementKey = "$.billing"
elementKeyType = "number"
[[projection.source.aggregate.element]]
field = "person_id"
from = "$.person_id"
[[projection.source.aggregate.element]]
field = "name"
lookup = "people"
on = "person_id"
select = "name"
