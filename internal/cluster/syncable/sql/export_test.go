package sql

// The [sql] vocabularies, exposed so the conformance test can prove each
// equals the keys the parsers actually read.
var (
	SyncableSQLKeys = syncableSQLKeys
	DatabaseSQLKeys = databaseSQLKeys
)
